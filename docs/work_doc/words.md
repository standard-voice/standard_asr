
Stop manually writing api servers for your asr/tts project.
Let your users use your ASR / TTS library like plugging usb devices.






ad/intro video for asr / tts builder

```
Standard ASR / TTS is very simple. It defines a standard way of interacting with ASR / TTS modules, so that the developers can write code once and run any ASR / TTS models they like.

For ASR / TTS developers like you, adopting standard asr / tts makes adoption much quicker and easier. Standard ASR also provides useful tools like CLI interface, web api server, test suite, and many more, so that you can focus on building your ASR, not writing REST APIs.
```


