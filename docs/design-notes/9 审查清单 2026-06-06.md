# Standard ASR Phase 2 审查清单

> 给你逐一审查用。每条标明：编号、标题、文件位置、审查要点。
> 经过多轮独立 subagent 设计、审查、修订、再验证,所有设计方向**零反转**,三基准(OpenAI/ElevenLabs/Qwen3)端到端通过。
>
> **文件位置**：
> - 规范 = `设计/设计规范.md`
> - 决策 = `设计笔记和决策/6 核心设计决策 2026-06-06.md`
> - 审查综合 = `设计笔记和决策/7 设计审查综合 2026-06-06.md`
> - 修订清单 = `设计笔记和决策/8 验证综合与修订清单 2026-06-06.md`
> - 差距分析 = `设计笔记和决策/5 差距分析 2026-06-05.md`
> - 调查 = `1 ASR 调查/调查笔记/` 下 1(大调查)、3(streaming 补查)、4(采样率)
> - README = 根目录 `README.md`

---

## 一、核心设计决策(D1–D10)

请对照 `决策` 文件中每条的「✅审查后定稿」批注。每条记录了:方向 / 得到 / 牺牲 / 备选 / 受影响利益相关者。

| # | 决策 | 文件位置 | 审查要点 |
|---|---|---|---|
| 1 | **D1 音频输入抽象** | 决策 D1 + 规范 §AI | 判别联合(5 种标签类型) + `accepted_input` 枚举 + 转换矩阵(穷举) + 死角 fail-explicit + array→WAV encoder(BytesIO/预检 max_file_size) + **AudioUrl v1 仅透传(SSRF 策略)**。便利 coercion 规则(裸 str 永不猜 URL)。 |
| 2 | **D2 采样率与 canonical 格式** | 决策 D2 + 规范 §AI.6–AI.7 + 采样率调查 | canonical 16kHz mono；裸数组 strict 报错/best_effort 带 diagnostic(**不静默**)；采样率字段归 Properties(不另造平行词汇)；8kHz 电话=preset 选择,禁升采样。 |
| 3 | **D3 重采样** | 决策 D3 + 规范 §AI.8 | `self_resamples: bool`(capability)；fallback **MUST 抗混叠**(FFT)；许可证排除(禁 vendoring soxr/SoX)；无 `[audio]` 走 fallback 带 diagnostic。 |
| 4 | **D4 numpy** | 决策 D4 + 规范 §DEP | interpreter-conditional floor(`>=1.26 py<3.13`/`>=2.1 py>=3.13`)；无上限(遵 SPEC-0)；clip/astype 防御；`standard-asr doctor`；进程隔离(subprocess+UDS 首选,非 FastAPI)。 |
| 5 | **D5 能力系统统一** | 决策 D5 + 规范 §C | 两层(`DeclaredCapabilities` ClassVar + `effective ⊆ declared`)；封闭 mode 域(batch/streaming)；节点三 archetype(flag/bounded/enum,每个 `.supported`)；fail-closed + 容忍未知键 + `x_*`。**废弃** FeatureFlag/feat_flag/per-feature version。 |
| 6 | **D6 runtime + guidance** | 决策 D6 + 规范 §R | `RuntimeParams` 封死(extra=forbid)；**v1 可移植集** = language/candidate_languages/word_timestamps/**prompt/phrase_hints**；`provider_params`(typed,always-raise,JSON-Schema-over-wire,标注非可移植)。**guidance 家族**(扁平字段+共享契约+不对称未知处理+opt-in 单向降级+`x_*`)。`bias_resource`→init config。 |
| 7 | **D7 Init Config** | 决策 D7 + 规范 §IC | `engine`=entrypoint 派生 id；**SecretStr 凭证**+脱敏序列化+write-only REST；env 回退 normalization+碰撞检测；适用性 mixin；**default_language 条件必填**(有语言轴就必给)；init/runtime 边界+tie-breaker。 |
| 8 | **D8 结果 shape 恒定** | 决策 D8 + 规范 §TR | schema 恒定；**null 规则**(capability=支持, None=未请求, []=请求但空)；时间=float 秒；多通道顶层=时序合并；speaker shape 预留；核心 `to_srt/to_vtt`。 |
| 9 | **D9 命名 Scheme A** | 决策 D9 | `start_transcription`→`TranscriptionSession`；`send_audio`/`end_audio`；`TranscriptionEvent(partial/final/supersede/progress/done/error)`。 |
| 10 | **D10 streaming** | 决策 D10 + 规范 §ST | **已通过 gate 验证**。全双工 session；稳定前缀(字素簇,只增不减)；supersede(核心事件,冻结前缀保留)；两级终态；音频时间游标；重连(按源分类,live mic 诚实丢失)；背压(合并被终态作废)；段间独立；segment_id 铸造规则；状态转移表。 |

## 二、规范各段(NORMATIVE)审查

| # | 规范段 | 文件位置 | 审查要点 | 模板重写状态 |
|---|---|---|---|---|
| 11 | **文档结构说明** | 规范 顶部 | 旧草稿(lines 1–300+)已标注「被取代」;NORMATIVE 章节清单;冲突以 NORMATIVE 为准。 | ✅ 已加 |
| 12 | **§AI 音频输入与采样率** | 规范 §AI.1–AI.10 | 5 种标签类型定义、转换矩阵(含死角)、encoder、SSRF、采样率、重采样、大媒体。 | ⬜ 待按模板重写 |
| 13 | **§LANG 语言选择** | 规范 §LANG | 术语表、声明表(Properties+Capabilities)、参数表、R1/R2/R3 解析算法、示例、附注。 | ✅ 已按模板重写+润色 |
| 14 | **§C 能力系统** | 规范 §C.1–C.6 | 两层能力、mode 域、节点 archetype、未知键/演进、app 访问、边界规则。 | ⬜ 待按模板重写 |
| 15 | **§R Runtime 参数 + guidance** | 规范 §R.1–R.6 | 封闭请求类型、v1 可移植集、provider_params 错误模型、guidance 家族(共享契约+降级+扩展)。 | ⬜ 待按模板重写 |
| 16 | **§TR 结果模型** | 规范 §TR.1–TR.7 | 恒定 schema、Segment/Word 流批共享、时间单位、多通道、speaker 预留、to_srt/to_vtt、演进规则。 | ⬜ 待按模板重写 |
| 17 | **§DEP 依赖与兼容** | 规范 §DEP.1–DEP.5 | numpy 条件 floor、稳定子集、不强制 2+、进程隔离、doctor。 | ⬜ 待按模板重写 |
| 18 | **§IC Init Config** | 规范 §IC.1–IC.10 | 判别器、凭证安全、env 回退、适用性、default_language、init/runtime 边界、多 artifact、lazy 纯度、bias_resource。 | ⬜ 待按模板重写 |
| 19 | **§ST 流式协议** | 规范 §ST 1–10 | 概述→术语→接口(含 `audio` 参数 fix)→事件(稳定前缀)→段生命周期(supersede+状态表)→健壮性(重连/背压/sync桥)→示例→附注→能力清单→v1。 | ✅ 已按模板重写+润色+gate 通过 |

## 三、Trade-off 确认(已拍板,供复核)

| # | Trade-off | 你的决定 | 记录位置 |
|---|---|---|---|
| 20 | `max_candidate_languages` 从 Properties 移入能力节点 `constraints.max` | 同意 | 决策 D5 批注 |
| 21 | `provider_params` 错误永远 raise,独立于 best_effort | 同意(代码 bug≠能力缺失) | 决策 D6 批注 |
| 22 | prompt 留核心,按 free-text/phrase/resource-id 三分 | 同意 | 审查综合 §8 |
| 23 | `mutable_mid_stream`(中途改热词)v1 砍(保 flag 不承诺方法) | 默认同意(你未反对) | 审查综合 §6 |
| 24 | 命名 `guidance`(非 `conditioning`) | 同意(conditioning 语义相反) | 审查综合 §8 |
| 25 | v1 scope「小而真的核心」 | 同意 | 审查综合 §6 |

## 四、调查资料(已完成,供参考)

| # | 文件 | 内容 |
|---|---|---|
| 26 | `1 大規模 ASR 調查 2026-05-09.md` | 17 引擎全面调查(特性矩阵+不可调和分歧+收敛) |
| 27 | `3 streaming 补充调查 2026-06-05.md` | ~20 引擎 streaming 行为补查(DSM/Canary/WeNet/Moonshine 等)；发现 Delayed Streams Modeling 新架构 |
| 28 | `4 採樣率調查報告.md` | 16kHz 事实标准实证 + 8kHz/24kHz 例外 + 引擎矩阵 |
| 29 | `README.md` | tentative README(extras 表、概念图、FAQ) |

## 五、后续工作(非本次审查范围,但已规划)

| # | 项 | 状态 |
|---|---|---|
| 30 | §AI/§C/§R/§TR/§DEP/§IC 按详细模板(概述→术语→声明→行为→示例→附注)重写 | 进行中 |
| 31 | 旧草稿内容并入 NORMATIVE 并清除旧表述 | 待做 |
| 32 | 标注被 supersede 的 idea_docs(options.md/results.md/streaming.md/features.md) | 待做 |
| 33 | 工作进度文档追加本轮记录 | 待做 |
