# standard_asr.wire

PCM wire codec for streaming: the canonical `pcm_s16le` encoding, and helpers for
encoding/decoding between float32 waveforms and little-endian 16-bit PCM bytes.

::: standard_asr.wire
