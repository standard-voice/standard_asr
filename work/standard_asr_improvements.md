# Standard ASR Improvement Ideas

- Documentation: CLI/transcribe walkthroughs should explicitly mention the
  `standard-asr[audio]` extra (or `scipy`) for resampling, since WAV inputs may
  require it even when FFmpeg is available.
